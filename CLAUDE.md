@AGENTS.md

## Claude Code

- Claude-specific skills, agents, hooks, and path-scoped rules live under `.claude/`.
- Use `/test`, `/release`, `/sync-check`, `/vibe-scene`, and `/vibe-pipeline` when relevant.
- Use `/memory` if instruction loading needs debugging.
